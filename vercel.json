{
  "builds": [
    {
      "src": "api/transcribe.js",
      "use": "@vercel/node"
    },
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "rewrites": [
    { "source": "/api/transcribe", "destination": "/api/transcribe" }
  ]
}